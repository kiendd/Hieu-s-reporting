# Tasks: add-docs-link

- [x] 1. Replace `st.title(...)` with a two-column row: left column = title, right column = markdown link to the docs URL
- [x] 2. Update `openspec/specs/web-ui/spec.md` with ADDED requirement
- [x] 3. Validate with `openspec validate add-docs-link --strict --no-interactive`
